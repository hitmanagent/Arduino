void setup() {

  Serial.begin (9600);
  // put your setup code here, to run once:
  {
    for (int x = 0; x < 10; x = x + 2 ) {
      Serial.println(x);
      //

      delay (500);
    }
    for (int x = 0; x < 10; x = x + 3 ) {
      Serial.println(x);
      delay (500);

    }
    for (int x = 0; x < 10; x = x + 4 ) {
      Serial.println(x);
      delay (500);

    }
    for (int x = 0; x < 10; x = x + 5 ) {
      Serial.println(x);
      delay (500);

    }
    for (int x = 0; x < 10; x = x + 6 ) {
      Serial.println(x);
      delay (500);

    }
    for (int x = 0; x < 10; x = x + 7 ) {
      Serial.println(x);
      delay (500);

    }
    for (int x = 0; x < 10; x = x + 8 ) {
      Serial.println(x);
      delay (500);

    }
    for (int x = 0; x < 10; x = x + 9 ) {
      Serial.println(x);
      delay (500);

    }
    for (int x = 0; x < 10; x = x + 10 ) {
      Serial.println(x);
      delay (500);

    }
    for (int x = 0; x < 10; x = x + 11 ) {
      Serial.println(x);
      delay (500);

    }
    for (int x = 0; x < 10; x = x + 12 ) {
      Serial.println(x);
      delay (500);

    }
    for (int x = 0; x < 10; x = x + 13 ) {
      Serial.println(x);
      delay (500);

    }
    for (int x = 0; x < 10; x = x + 14 ) {
      Serial.println(x);
      delay (500);

    }
    for (int x = 0; x < 10; x = x + 15 ) {
      Serial.println(x);
      delay (500);

    }
    for (int x = 16; x < 10; x = x + 16 ) {
      Serial.println(x);
      delay (500);

    }
    for (int x = 17; x < 10; x = x + 17 ) {
      Serial.println(x);
      delay (500);

    }
    for (int x = 18; x < 10; x = x + 18 ) {
      Serial.println(x);
      delay (500);

    }
    for (int x = 19; x < 10; x = x + 19 ) {
      Serial.println(x);
      delay (500);

    }
  }





}


void loop() {
  // put your main code here, to run repeatedly:

}
